# <project name>

**Question:** <the one question this project answers, in a sentence>

**Status:** TODO | running | explored | promising | null-result | parked-pending-data

## Plan
<one paragraph: what gets computed and how, which existing repo loaders it reuses>

## How to run
```
python EXPLORATORY/<project>/run.py
```
Outputs land in `outputs/`. Findings in `FINDINGS.md`.

## Effort box
Get the first real result (see below), verify it, write findings, stop. Do not
gold-plate. If the first result is promising, note the next step in FINDINGS
under "How to extend" rather than building it now.

**First real result for this project:** <the single concrete artifact that means
this project has produced something worth reading, e.g. "signed % error per
estimation level, per part, in a table" or "per-operation run-order trend table
plus the grouped figure">

## Dependencies beyond the repo's existing stack
<none, or list>
