# <project name>: findings

**Status:** explored | promising | null-result | parked-pending-data
**Date:** <YYYY-MM-DD>

## Headline
<the result in 2 to 3 sentences, written so the repo owner's advisors could read
it. State the number, its n, and its main caveat in the same breath. If null,
say so plainly: a null result here means <what it rules out>.>

## Numbers
<table or short list of the computed values, each traceable to the script and
the output CSV. Mark any value that disagrees with a previously quoted number.>

## Verification
<paste the cross-check table from CheckLog.to_markdown(). Note any DISAGREE rows
and what you concluded.>

## Caveats
<the honest limitations, next to nothing else. Single machine, single material,
synthetic aggregation, thin validation set, etc., as applicable.>

## Feeds the journal paper?
<which manuscript section this could support, and the specific sentence or number
it provides. "None" is a valid answer.>

## How to extend
<the obvious next step if someone grows this seed later. One or two bullets.>
