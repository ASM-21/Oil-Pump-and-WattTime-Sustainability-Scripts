# explorations/

Short notes from the cheap exploration pass, one file per direction explored
(e.g. `A1_utilization_landscape.md`). Each note records: what was checked,
what the existing numbers or a quick literature scan showed, and whether the
direction has enough substance to justify a real build. Killing a direction
fast and clearly is a successful note.
